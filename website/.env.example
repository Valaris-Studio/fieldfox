# Optional HTTPS booking link, compiled into the public site.
# VITE_FIELDFOX_MEETING_URL=
# Server-only receiver configuration: see server/README.md.
# Enquiry delivery is deliberately unconfigured by default.
